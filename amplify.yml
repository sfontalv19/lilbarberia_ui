version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        # 👇 construye tu aplicación Next.js (App Router)
        - npm run build
  artifacts:
    # 👇 esta es la carpeta correcta para SSR (no .next)
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
  customHeaders:
    - pattern: '**/*'
      headers:
        - key: 'Cache-Control'
          value: 'no-cache'
  # 👇 fuerza ejecución del servidor Next.js en Amplify
  custom:
    nextJsBuild:
      enableSSR: true
