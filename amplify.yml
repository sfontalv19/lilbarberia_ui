version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # 👇 Amplify debe publicar la carpeta generada por `next export`
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
